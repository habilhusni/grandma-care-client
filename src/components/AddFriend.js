import React from 'react'
import {  } from 'react-native'
import { Container, Content, Header, Left, Body, Right, Input, Form, Icon, Text } from 'native-base'

import { connect } from 'react-redux'
import PropTypes from 'prop-types'

import {} from '../actions'

class UserList extends React.Component {

  render() {
    const {} = this.props
    return (
      <Container>
        <Header>

        </Header>
        <Content>

        </Content>
      </Container>
    )
  }
}